rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Herhangi bir koleksiyondaki tüm dökümanlar:
    match /{document=**} {
      // Sadece giriş yapmış kullanıcılar okuyup/yazabilir
      allow read, write: if request.auth != null;
    }
  }
}
